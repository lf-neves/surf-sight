{
  "name": "forecast-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "build": "rm -rf dist && tsc",
    "type-check": "tsc --noEmit",
    "create-test-event": "pnpm --filter @surf-sight/database db:generate && tsx scripts/create-test-event.ts",
    "invoke:enqueue": "pnpm --filter @surf-sight/database db:generate && serverless invoke local -f enqueueForecasts",
    "invoke:process": "pnpm --filter @surf-sight/database db:generate && serverless invoke local -f processSqsMessage --path events/sqs-event.json",
    "package": "pnpm --filter @surf-sight/database db:generate && serverless package",
    "deploy": "pnpm --filter @surf-sight/database db:generate && serverless deploy"
  },
  "dependencies": {
    "@surf-sight/core": "workspace:*",
    "@surf-sight/database": "workspace:*",
    "@surf-sight/test-setup": "workspace:*"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.10.2",
    "serverless": "^4.0.0",
    "ts-jest": "^29.2.5",
    "tsx": "^4.20.6",
    "typescript": "^5.7.3"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
